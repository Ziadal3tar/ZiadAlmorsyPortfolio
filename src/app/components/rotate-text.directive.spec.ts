import { RotateTextDirective } from './rotate-text.directive';

describe('RotateTextDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateTextDirective();
    expect(directive).toBeTruthy();
  });
});
